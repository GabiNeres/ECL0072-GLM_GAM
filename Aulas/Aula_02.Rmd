---
title: "Modelos Lineares Generalizados e Aditivos para Dados Ecológicos (ECL0072)"
subtitle: "Aula 02: Métodos de inferência, diagnóstico & seleção de modelos"
author: "Marie-Christine Rufener"
#institute: "macrufener@gmail.com"
#date: "2016/12/12 (updated: `r Sys.Date()`)"
output:
  xaringan::moon_reader:
    css: [default, metropolis, metropolis-fonts, my-theme.css]
    lib_dir: libs
    nature:
      #seal: false #disable title slide
      highlightStyle: github
      highlightLines: true
      countIncrementalSlides: false
---




## <span style="color: #ee6c4d;">Conteúdo programático </span> 

<br>

**1. Métodos de inferência**  

  > Prática 

**2. Diagnóstico de modelos** 


**3. Seleção de modelos** 

  > Prática 

<br>



---
## <span style="color: #ee6c4d;">Leitura sugerida </span> 

**Livros**
- **Caps. 6 & 10:** Faraway (2014)
- **Caps. 8 & 11:** Gelman et al. (2020)
- **Caps. 6 & 7:** Bolker (2008)
- **Cap. 6:** Clark & Berry (2025)


-----
**Artigos**

- Zuur, A.F. & Ieno, E.N. (2016). A protocol for conducting and presenting results of regression‐type analyses. Methods in Ecology and Evolution. https://doi.org/10.1111/2041-210X.12577


---
class: inverse, center, middle

#  Métodos de inferência

---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span>

<br>

```{r echo=FALSE, out.width ="80%", fig.align ='center'}
knitr::include_graphics("../Figuras/LM_ajustado2.png")
```


---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span>


#### Como estimamos (inferimos) as relações da natureza?

<br>

--

- 1) Definir o modelo que descreve o sistema em estudo (i.e., distribuição de probabilidade)
    - Função será utilizada para predições

--

<br>

- 2) Identificar possíveis valores para os parâmetros desconhecidos do modelo 

--

<br>

- 3) Avaliar a incerteza dos parâmetros estimados
    - Exploar a função em torno de valores de parâmetros plausíveis (espaço paramétrico; *parameter space*)

---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span>

#### Como estimamos (inferimos) as relações da natureza?

<br>

- **2) Identificar possíveis valores para os parâmetros desconhecidos do modelo**

    - Método dos momentos
    - <span style="color: #ee6c4d;">Método dos mínimos quadrados</span>
    - <span style="color: #ee6c4d;">Método de máxima verossimilhança</span>
    - Método bayesiano

---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Mínimos quadrados (OLS)  </span> 


.pull-left[

<br>


```{r echo=FALSE, out.width ="80%", fig.align ='center', fig.cap="Carl F. Gauss (1777-1855)", fig.topcaption=F}
knitr::include_graphics("../Figuras/Gauss.png")
```

]

.pull-right[


```{r echo=FALSE, out.width ="80%", fig.align ='center'}
knitr::include_graphics("../Figuras/Ceres_sketch.png")
```

]

---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Mínimos quadrados (OLS)  </span> 


.pull-left[

<br>
<br>

```{r echo=FALSE,fig.align ='center'}
knitr::include_graphics("../Figuras/Exemplo_OLS.png")
```

]


.pull.right[


<br>
<br>


$$\large y_i = f(x_i | \theta) + \varepsilon_i$$
<br>

$$\large \varepsilon_i = y_i - f(x_i|\theta)$$

]


.pull-right[
<p class="comment">

<b>Lembrando que:</b>

$$ f(x_i|\theta) = \beta_0 + \beta_1x_i = \mu_i \\
i = 1, ..., n
$$
</p>
]


---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Mínimos quadrados (OLS)  </span> 

<br>

.pull-left[
```{r echo=FALSE,fig.align ='center', out.width ="100%"}
knitr::include_graphics("../Figuras/exemplo_ajuste_LM.png")
```

]

.pull-right[


<br>
<br>

<p class="comment">

<b>Achar a linha que melhor se ajusta aos dados!</b>

</p>


]


---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Mínimos quadrados (OLS)  </span> 

<br>

$$\large y_i = f(x_i | \theta) + \varepsilon_i$$


$$\large \varepsilon_i = y_i - f(x_i|\theta)$$

<br>


$$\large \text{SQR} = min \sum_{i=1}^{n} (y_i - f(x_i|\theta))^2$$


---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Mínimos quadrados (OLS)  </span> 

<br>

$$\large y_i = f(x_i | \theta) + \varepsilon_i$$

--

$$\large \varepsilon_i = y_i - f(x_i|\theta)$$

<br>

$$\large \text{SQR} = \underbrace{min \sum_{i=1}^{n} (y_i - f(x_i|\theta))^2}_
{\text{Diferenciar a função em relação a }\ \theta}$$

---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Mínimos quadrados (OLS)  </span> 

**Solução analítica:**

$$\large \hat{\beta_0} = (X^TX)^{-1}X^Ty ~~\color{gray}{(= \overline{y} - \widehat{\beta_1}~\overline{x})}$$
--

<br>

$$\large \hat{\beta_1} = X(X^TX)^{-1}X^Ty ~~\color{gray}{(= r \frac{\sigma_y}{\sigma_x})}$$

--

<br>

$$\large \hat{y} = Hy ~~\color{gray}{(= \hat{\beta_1})}$$




.footnote[**H** = matriz 'hat'; **r** = coeficiente de correlação;  **σ** = desvio padrão]


---

### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 
<br>

.pull-left[
<br>

- Formalizou a teoria de máxima verossimilhança 
  - **estatística frequentista**
      - <span style="color: #ee6c4d;">Intervalo de confiança</span>
      - <span style="color: #ee6c4d;">Teste de hipótese (p-valor...)</span>
      - <span style="color: #ee6c4d;">Comparação entre modelos</span>



]

.pull-right[

```{r echo=FALSE, out.width ="66%", fig.align ='center', fig.cap="Ronald A. Fisher (1890-1962)", fig.topcaption=F}
knitr::include_graphics("../Figuras/Ronald_Fisher.jpeg")
```


]

---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 

#### Entendendo o método:

<br>

- Os dados (i.e, $Y$) surgem de um processo aleatório
  - podem ser descritos por um modelo probabilístico!

---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 

#### Entendendo o método:

$$\large Y \sim N(\mu, \sigma^2)$$


```{r echo=FALSE, out.width ="80%", fig.align ='center'}
knitr::include_graphics("../Figuras/Dist_normal.png")
```


---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 

#### Entendendo o método:
<br>

- Dado o modelo probabilístico, deseja-se encontrar os valores dos parâmetros ($\theta$) que mais provavelmente geraram os dados observados


---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### Confrontando os dados com a distribuição de probabilidade

<br>

```{r echo=FALSE, out.width ="70%", fig.align ='center'}
knitr::include_graphics("../Figuras/MLE_exemplo.png")
```


---

### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### Confrontando os dados com a distribuição de probabilidade

<br>

```{r echo=FALSE, out.width ="70%", fig.align ='center'}
knitr::include_graphics("../Figuras/MLE_exemplo2.png")
```


---

### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### Confrontando os dados com a distribuição de probabilidade

<br>

```{r echo=FALSE, out.width ="70%", fig.align ='center'}
knitr::include_graphics("../Figuras/MLE_exemplo3.png")
```

---

### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### Confrontando os dados com a distribuição de probabilidade

<br>

```{r echo=FALSE, out.width ="70%", fig.align ='center'}
knitr::include_graphics("../Figuras/MLE_exemplo4.png")
```

---

### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### Confrontando os dados com a distribuição de probabilidade


```{r echo=FALSE, out.width ="90%", fig.align ='center'}
knitr::include_graphics("../Figuras/MLE_gif.gif")
```

---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### Confrontando os dados com a distribuição de probabilidade

.pull-left[
<br>

```{r echo=FALSE, out.width ="100%", fig.align ='center'}
knitr::include_graphics("../Figuras/Exemplo_mle_mu.png")
```
]
.pull-right[

<br>
<br>
<br>
<br>

$$\large \hat{\theta} := argmax ~ L(\theta)$$
]

---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### Confrontando os dados com a distribuição de probabilidade

Como a distribuição Normal é descrita por dois parâmetros ($\mu,\sigma^2$), é preciso achar o máximo da função de verossimilhança *simultaneamente* para ambos os parâmetros

.pull-left[

```{r echo=FALSE, out.width ="80%", fig.align ='center'}
knitr::include_graphics("../Figuras/Exemplo_mle_mu.png")
```
]

.pull-right[

```{r echo=FALSE, out.width ="80%", fig.align ='center'}
knitr::include_graphics("../Figuras/Exemplo_mle_sigma.png")
```
]

---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### Confrontando os dados com a distribuição de probabilidade


```{r, echo=F, message=F, warning=F}
set.seed(1)
y <- rnorm(1000, mean = 5, sd = 2)

# Common grid
mu_seq    <- seq(4.75,  5.15,  length.out = 100)
sigma_seq <- seq(1.95, 2.25, length.out = 100)

# Common log-likelihood function
loglik_normal <- function(mu, sigma, y) {
  if (sigma <= 0) return(NA_real_)
  sum(dnorm(y, mean = mu, sd = sigma, log = TRUE))
}

# Grid for contour
grid <- expand.grid(mu = mu_seq, sigma = sigma_seq)
grid$loglik <- mapply(loglik_normal, grid$mu, grid$sigma, MoreArgs = list(y = y))
grid$lik <- exp(grid$loglik - max(grid$loglik, na.rm = TRUE))

mu_hat    <- mean(y)
sigma_hat <- sqrt(mean((y - mu_hat)^2))
nll_hat   <- -loglik_normal(mu_hat, sigma_hat, y) # neg LL for 3D
```

.pull-left[
<br>

```{r, echo=F, message=F, warning=F,  fig.asp=0.9}
library(ggplot2)

ggplot(grid, aes(x = mu, y = sigma, z = lik)) +
  geom_contour_filled() +
  geom_vline(xintercept = mu_hat, color = "white", size = .4) +
  geom_hline(yintercept = sigma_hat, color = "white", size = .4) +
  scale_fill_viridis_d(option = "D", direction = 1) +
  labs(title = "Verossimilhança", x = expression(mu), y = expression(sigma^2), fill = NULL) +
  theme_minimal(base_size = 14) +
  theme(plot.title = element_text(hjust = 0.5, face='bold'))
```


]
.pull-right[
<br>

```{r,echo=F, message=F, warning=F, fig.asp=0.7, fig.align='left'}
library(plotly)

Z <- outer(mu_seq, sigma_seq, Vectorize(function(m, s) -loglik_normal(m, s, y)))


plot_ly(
  x = ~mu_seq, y = ~sigma_seq, z = ~t(Z)
) |>
  add_surface(showscale = FALSE, opacity = 0.8, reversescale = T) |>
  layout(scene = list(
    xaxis = list(title = "mu"),
    yaxis = list(title = "sigma"),
    zaxis = list(title = "Neg. log lik")
  )) |>
  add_markers(
    x = mu_hat, y = sigma_hat, z = nll_hat,
    marker = list(size = 6, symbol = "circle", opacity = 1),
    showlegend = FALSE) %>%
  layout(margin = list(t = 0, b = 20, l = 0, r = 0))


```


]


---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


<p class="comment">
<b>ATENÇÃO</b>
</p>

$$\large \text{Verossimilhança} \neq \text{Probabilidade}$$ 


---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


```{r echo=FALSE, out.width ="100%", fig.align ='center'}
knitr::include_graphics("../Figuras/Probabilidade_verossimilhanca.png")
```



---

### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### Traduzindo para o matematiquês



<br>

$$\large L(\theta \mid y_1, \dots, y_n) = \prod_{i=1}^{n} f(y_i \mid \theta)$$
--

$$\large L(\theta \mid y_1, \dots, y_n) = f(y_1 \mid \theta) \cdot f(y_2 \mid \theta) \cdot f(y_3 \mid \theta) \cdots f(y_n \mid \theta)$$
<br>

--


**Quando** $\bf{n >>}$
- limitação computacional
    - truque matemático: aplicar o log!
    

---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### Traduzindo para o matematiquês

<br>

$$\large L(\theta \mid y_1, \dots, y_n) = \prod_{i=1}^{n} f(y_i \mid \theta)$$

--

Função de log-verossimilhança (*log-likelihood*)

$$\large \log L(\theta\mid y_1,\dots,y_n)
=\sum_{i=1}^n \log f(y_i\mid \theta)$$

--

- Tornando o produto em somatório implica em:
    - Estabilidade numérica e maior facilidade na diferenciação da função
    - O máximo da função torna-se o mínimo, i.e., $\hat{\theta} := argmin ~ L(\theta)$


---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### Verossimilhança perfilada (*profile likelihood*)

<br>

```{r echo=FALSE, out.width ="100%", fig.align ='center'}
knitr::include_graphics("../Figuras/profile_likelihood.png")
```

<p class="comment">
<b>ATENÇÃO</b>
<br>

A curvatura da função está relacionada à precisão do parâmetro estimado! 
</p>


---

### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### **Exemplo prático**

O histograma *parece* indicar que o peso dos pinguins, $y_i$, pode ser descrito conforme uma distribuição Normal (Gaussiana)

.pull-left[

```{r echo=FALSE, out.width ="100%", fig.align ='center'}
knitr::include_graphics("../Figuras/histograma_pinguins.png")
```

]

.pull-right[

<br>
<br>

$$\Large Y \sim N(\mu, \sigma^2)$$
<br>

> Como estimar os parâmetros? 

]

---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### **Exemplo prático**

Lembrando que a função de verossimilhança $L(\cdot)$ é descrita em relação ao modelo probabilístico que descreve a variável $f(Y|\theta)$

$$\large L(\theta \mid y_1, \dots, y_n) = \prod_{i=1}^{n} \color{Tomato}{f(y_i \mid \theta)}$$
--

Para a distribuição Normal, temos:


$$\large L(\mu, \sigma^2 \mid Y) = \prod_{i=1}^n \color{Tomato}{\frac{1}{\sqrt{2\pi\sigma^2}} 
\exp\left[ -\frac{(y_i - \mu)^2}{2\sigma^2} \right]}$$

--


---
### <span style="color: #ee6c4d;">**Métodos de inferência** </span><span style="color: #3e5c76;">| Máxima verossimilhança  </span> 


#### **Exemplo prático**

<br>
<br>

```{r echo=FALSE, out.width ="25%", fig.align ='center', fig.cap="Pratica_aula_02.R", fig.topcaption=F}
knitr::include_graphics("../Figuras/Rlogo.png")
```


---
class: inverse, center, middle

#  Diagnóstico de modelos
---

### <span style="color: #ee6c4d;">**Diagnóstico de modelos** </span>


Tem por objetivo avaliar os pressupostos e a qualidade do ajuste do modelo
- o quão bem o modelo se ajustou aos dados

--

<br>

Realizado mediante a análise residual

.pull-left[
- LM $\neq$ GLM

- No caso de LM:
  - Independência
  - Homocedasticidade
  - Normalidade
]

--

.pull-right[

**Métodos de diagnóstico**

- Numérico (validação estatística)
- Gráfico (validação visual)

]

---
### <span style="color: #ee6c4d;">**Diagnóstico de modelos** </span><span style="color: #3e5c76;">| Homocedasticidade</span> 

A constância da $\sigma^2$ implica que os resíduos precisam estar simetricamente distribuídos em torno da linha horizonthal $y=0$ 

```{r echo=FALSE, out.width ="90%", fig.align ='center'}
knitr::include_graphics("../Figuras/heterocedasticidade_plots.png")
```




---
### <span style="color: #ee6c4d;">**Diagnóstico de modelos** </span><span style="color: #3e5c76;">| Independência</span> 

<br>

$$\Large \epsilon \overset{\mathrm{iid}}{\sim} N(0,\sigma^2)$$

<br>

A violação da independência ocorre frequentemente por dados com estrutura:
- espacial
- temporal
- em blocos


---

### <span style="color: #ee6c4d;">**Diagnóstico de modelos** </span><span style="color: #3e5c76;">| Independência</span> 


Se os dados foram coletados no espaço geográfico (long/lat), deve-se avaliar como os resíduos se comportam espacialmente
  - **Validação visual:** *bubbleplot*
  - **Validação estatística:** Morans' I 


---
### <span style="color: #ee6c4d;">**Diagnóstico de modelos** </span><span style="color: #3e5c76;">| Independência</span> 


```{r echo=FALSE, out.width ="100%", fig.align ='center'}
knitr::include_graphics("../Figuras/corr_espacial.png")
```

---
### <span style="color: #ee6c4d;">**Diagnóstico de modelos** </span><span style="color: #3e5c76;">| Independência</span> 

Caso a coleta de dados foi realizada ao longo do tempo, avalia-se o padrão temporal dos resíduos

  - **Validação visual:** ACF plot
  - **Validação estatística:** Durbin-Watson

---
### <span style="color: #ee6c4d;">**Diagnóstico de modelos** </span><span style="color: #3e5c76;">| Independência</span> 

<br>

```{r echo=FALSE, out.width ="100%", fig.align ='center'}
knitr::include_graphics("../Figuras/corr_temporal.png")
```

---
### <span style="color: #ee6c4d;">**Diagnóstico de modelos** </span><span style="color: #3e5c76;">| Normalidade</span> 


<br>

```{r echo=FALSE, out.width ="90%", fig.align ='center'}
knitr::include_graphics("../Figuras/normalidade_plots.png")
```


---
class: inverse, center, middle

#  Seleção de modelos


---

### <span style="color: #ee6c4d;">**Seleção de modelos** </span>


.pull-left[

<br>

```{r echo=FALSE, out.width ="60%", fig.align ='center', fig.cap="George P. Box (1919-2013)", fig.topcaption=F}
knitr::include_graphics("../Figuras/GeorgeEPBox.jpg")
```


]

.pull-right[

<br>
<br>
<br>
<br>

> <font size="6"><center> <span style="color: #ee6c4d;">Essentially, <b>all models are wrong</b>, but <b>some are useful</b></center></font></span> 

]

---

### <span style="color: #ee6c4d;">**Seleção de modelos** </span>

Baseado no *princípio da parcimônia*
  - Modelo mais simples com maior poder explicativo

--

<br>

Existem diversas métricas que podem ser utilizadas para comparar diferentes modelos:

- Deviance
- Teste da razão de verossimilhança (LRT)
- Critérios de informação (AIC, BIC)
- Ajuste gráfico


---

### <span style="color: #ee6c4d;">**Seleção de modelos** </span><span style="color: #3e5c76;">| Deviance  </span>


Generalização da soma dos quadrados dos resíduos (RSS):

<br>

$$\large D = 2 \times (logL_{saturado}- logL_{proposto})$$
- $logL_{saturado}$: log-verossimilhança modelo saturado (ajuste perfeito)
- $logL_{proposto}$: log-verossimilhança modelo proposto

<br>

--

> <span style="color: #ee6c4d;">**Interpretação:** quanto menor a deviance, mais próximo o modelo está do ajuste perfeito</p>

<br>

--

Deviance nula *vs.* deviance residual

---

### <span style="color: #ee6c4d;">**Seleção de modelos** </span><span style="color: #3e5c76;">| Teste da razão de verossimilhança  </span>


Usado para comparar modelos aninhados

- $\text{modelo A:}~~~ \large y_i = \beta_0 + \beta_1x_{i_1}$

- $\text{modelo B:}~~~ \large y_i = \beta_0 + \beta_1x_{i_1} + \beta_2x_{i_2}$


--


Baseado na estatística do teste da razão de verossimilhança:

.pull-left[
$$G^2 = 2 \times log\Biggl(\frac{L_B}{L_A}\Biggr)$$
$$G^2 = \underbrace{2 \times (logL_A - logL_B)}_\text{Deviance}$$

]
.pull-right[

- $G^2$ segue uma distribuição do qui-quadrado ($\chi^2$) com n = ($n_{p_A} - n_{p_B}$) graus de liberdade

]

--

> <span style="color: #ee6c4d;">**Interpretação:** se p < 0.05 rejeita-se a hipótese nula (modelo mais simples); modelo mais complexo se ajusta melhor aos dados!</p>


---

### <span style="color: #ee6c4d;">**Seleção de modelos** </span><span style="color: #3e5c76;">| Critérios de informação  </span>


Avalia não apenas o ajuste do modelo, como também a sua complexidade
  - penalização pelo número de parâmetros


--

.pull-left[

**AIC (Akaike Information Criterion)**

<br>

$$\large AIC = -2log(L_{modelo}) + 2k$$
$k:$ número de parâmetros


>   <span style="color: #ee6c4d;">**Interpretação:** quanto menor AIC, melhor o ajuste</p>

]

--

.pull-right[

**BIC (Bayesian Information Criterion)**

<br>

$$\large BIC = -2log(L_{modelo}) + k~ log(n)$$
$n:$ número de observações


>  <span style="color: #ee6c4d;">**Interpretação:** quanto menor BIC, melhor o ajuste</p>

]

---

```{r echo=FALSE, out.width ="80%", fig.align ='center'}
knitr::include_graphics("../Figuras/modelling_workflow.png")
```


---
